# TestFlight 1.1 (81)

Джерело архіву: `d8dd80a1591bedfe9d129e6c5e5766b81d662e96` (09.09.2026).

Містить виправлення після build 80: відступи налаштувань і деталей контенту, актуалізацію «Моїх організацій» після видалення, розділення помилок сповіщень, локалізовані описи помилок, завершення підписок коментарів перед видаленням, зрозуміле подання журналу й поточний профіль користувача в деталях.

Цільові перевірки виконані перед архівуванням: 72 тести для сповіщень/коментарів/локалізації, повторні 13 тестів сповіщень після виправлення скасування, 17 тестів нового журналу. Це окремі запуски з частковим перекриттям; їх не підсумовано як унікальні сценарії.

Release-архів зібрано; версія 1.1, номер 81, підпис перевірено, `ITSAppUsesNonExemptEncryption=false`. Архів і журнали зберігаються поза Git: `~/Library/Developer/UAC-Releases/1.1-81/`.

Основна папка на MacBook Pro `/Users/serlest/Developer/UkrainianCommunity` переведена на актуальну гілку `codex/macbook-current-1.1-81`. Попередні незакомічені зміни й untracked-файли збережені локально: `refs/backups/macbook-before-1.1-81` (stash `3dbec7bbefe5fd57d83fc0cec876faba372f77e9`). Початкова гілка `integration/local-product-progress` збережена.

TestFlight: Apple прийняла білд — `VALID`, `IN_BETA_TESTING`, `INTERNAL_ONLY`; build ID `39e051bd-f0d3-4b59-8510-12c3c1dbaca0`. Внутрішня група `Beta Tester` має доступ до всіх білдів. Лише внутрішнє тестування; публічного випуску або зовнішнього Beta Review немає. Інструкції для перевірки додано українською (`uk`) та німецькою (`de-DE`). Перевірка користувачем на фізичному пристрої — після встановлення.

Apple повідомила про відсутні dSYM п’яти сторонніх Firebase/gRPC-бібліотек. Завантаження успішне; це обмеження діагностики збоїв цих бібліотек. Нові переклади помилок і журналу перевірено безпосередньо в архіві для uk/de/en.
