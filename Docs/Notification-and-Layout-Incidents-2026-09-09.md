# Сповіщення, коментарі та відступи — 09.09.2026

## Підтверджено й виправлено в коді

- **Сповіщення:** список і серверний лічильник непрочитаних використовували спільну помилку. Окремий збій лічильника міг показати банер над успішно завантаженими картками. Тепер тимчасова мережева помилка лічильника зберігає відомий стан; відмови доступу та помилки запиту залишаються видимими. Помилки списку, лічильника й операцій розділено за джерелом; успішна підписка не стирає помилку невдалої дії. Додано первинну діагностику `Notifications.fetchUnreadCount`.
- **Мова:** `AppError` не реалізовував `LocalizedError`, тому Foundation показував системний опис німецькою. П'ять типів помилок тепер мають тексти українською, німецькою та англійською відповідно до мови застосунку.
- **Коментарі:** підписка залишалася активною під час видалення новини або скасування події, коли батьківський об'єкт уже ставав недоступним. Підписку тепер знімаємо до запиту; після невдачі відновлюємо лише для відкритого екрана й тієї самої сесії. Правила доступу не послаблено.
- **Відступи:** раніше виправлений `PushedScreenShell` покриває сповіщення та деталі журналу на фото. Такий самий дефект знайдено й виправлено в `DetailScreenShell`: відступ унизу застосовувався до окремих дочірніх блоків. Тепер він застосовується один раз до спільного контейнера. Перевірені споживачі: деталі події, новини й організації. `EditorScreenShell` і `AdminScreenShell` уже мають спільний контейнер.

## Перевірка робочого сервера

- Точний запит `isRead == false AND archivedAt == null AND deletedAt == null` успішний для двох акаунтів зі сценарію; відповідний індекс `READY`. Read-only адміністративна перевірка 17:15 за Віднем; це не перевірка мережі телефона або клієнтського токена. Локальні Rules дозволяють читання власної скриньки через `isSelf(uid)`.
- Новину видалено 16:23:30.102; помилка її коментарів — 16:23:32.040. Скасування події почалося 16:23:38.772; помилка коментарів — 16:23:39.110. Журнал і серверний код підтверджують причину.
- `getBlockedOrganizations` о 16:33:08: клієнтський `NSURLErrorDomain -1005`, розрив з'єднання через 13.6 с. Сусідні серверні запити — HTTP 200. Причину розриву на телефоні не встановлено; зміни backend для цього запису не внесено.

## Межі доказів

Точний первинний збій сповіщень о 16:17 та 16:59 **не встановлений**: запит лічильника раніше не записував технічну причину. Знайдений дефект стану підтверджено кодом і регресійними сценаріями, але він не доводить причину кожного історичного випадку. Нова діагностика потрібна для однозначного розбору повторення.

Центральна перевірка: 72 тести пройшли, 0 помилок (AppErrorLocalizationTests, NotificationInboxViewModelTests, ContentInteractionRaceTests, EventRegistrationRaceTests). Результат: `test_sim_2026-09-09T15-13-53-365Z_pid52977_ef23373b.xcresult`. Є попередження concurrency у MockRepositoryStore; перевірка не заявляє їх виправлення.

Після виправлення скасування фонового запиту повторна перевірка NotificationInboxViewModelTests: 13 тестів пройшли, 0 помилок. Результат: `test_sim_2026-09-09T15-17-00-120Z_pid52977_1921d404.xcresult`. Звичайне скасування більше не відображається й не журналюється як невідома помилка.

Ці зміни ще не входять до встановленого TestFlight 1.1 (80). Візуальну перевірку всіх екранів і повторення на фізичному телефоні з новим бінарним файлом у цьому пакеті не виконано.
